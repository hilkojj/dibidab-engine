config:
  hpp_incl:
    - input/key_input.h
    - input/gamepad_input.h

KeyListener:
  _cpp_only:
    keys: std::map<std::string, KeyInput::Key *>

GamepadListener:
  gamepad: uint
  _cpp_only:
    buttons: std::map<std::string, GamepadInput::Button *>

