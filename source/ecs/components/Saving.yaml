config:
  hpp_incl:
    - "../ecs/PersistentEntityRef.h"

Persistent:
  persistentId: PersistentEntityID

  applyTemplateOnLoad: std::string
  data: [ json, json::object() ]

  saveFinalPosition: [ bool, false ]
  saveSpawnPosition: [ bool, false ]
  spawnPosition: [ vec3, vec3(0) ]

  revive: [ bool, false ]
  saveAllComponents: [ bool, false ]
  saveComponents: std::vector<std::string>

  saveName: [ bool, true ]
